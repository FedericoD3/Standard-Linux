# Apagar los procesos del panel y eliminar la configuración:
  xfce4-panel --quit 
  pkill xfconfd 
  rm -rf ~/.config/xfce4/panel
  rm -rf ~/.config/xfce4/xfconf/xfce-perchannel-xml/xfce4-panel.xml
# Reiniciar el panel, que regenera la configuración default:
   xfce4-panel &
