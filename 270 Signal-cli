# MENSAJERIA SIGNAL POR COMANDOS
# útil para reportar statuses

# https://github.com/AsamK/signal-cli/wiki/Provide-native-lib-for-libsignal

# Instalar OpenJRE porque el Java oficial de Oracle está bloqueado para Venezuela:
  sudo apt update
  sudo apt install openjdk-17-jre

# Ver versión mas reciente de signal-cli en https://github.com/AsamK/signal-cli/releases/ y asignarla a una variable
  VERSION=0.13.18
  wget https://github.com/AsamK/signal-cli/releases/download/v"${VERSION}"/signal-cli-"${VERSION}".tar.gz
  sudo tar xf signal-cli-"${VERSION}".tar.gz -C ~/SignalCli
