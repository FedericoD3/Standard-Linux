# UTILITARIOS BASICOS

# Asegurar que estén actualizadas las fuentes:
  sudo apt update
  sudo apt upgrade -y

# Basicos:
  sudo apt install curl wget zip unzip

# nala 
# (interfaz más informativa de apt para instalar paquetes)
  curl https://gitlab.com/volian/volian-archive/-/raw/main/install-nala.sh | bash
# de aquí en adelante se puede usar "sudo apt ..." o "sudo nala ..." 

# Confirmador de reseteos y apagados remotos ( https://adrianroselli.com/wp-content/uploads/2024/02/switch-with-molly-guard.jpg ):
  sudo apt install molly-guard

# Cronometrador de procesos:
  apt install hyperfine
  # Uso: hyperfine --runs 1 <comando>
  #      hyperfine --min-runs 5 --max-runs 10 <comando>

# Navegador de archivos:
  sudo apt install ranger

# Fonts:
  # Preparar un directorio ordenado para los fonts monoespaciados extra:
    sudo mkdir -p /usr/share/fonts/Extra/Monoesp
  # Victor Mono:
    wget https://rubjo.github.io/victor-mono/VictorMonoAll.zip -O ~/Downloads/VictorMonoAll.zip
    sudo unzip ~/Downloads/VictorMonoAll.zip -d /usr/share/fonts/Extra/Monoesp/VictorMono
    rm ~/Downloads/VictorMonoAll.zip
  # Refrescar el cache de fonts:
    fc-cache -f -v

# Multi terminal moderno, verstil y multiplataforma ( https://tabby.sh https://github.com/Eugeny/tabby ):
  wget https://github.com/Eugeny/tabby/releases/download/v1.0.225/tabby-1.0.225-linux-x64.deb -O ~/Downloads/Tabby.deb
  sudo apt install ~/Downloads/Tabby.deb
  rm ~/Downloads/Tabby.deb
# ahora se puede empezar a usar tabby en lugar de uxterm o lxterm o lo que haya traído la distro

# Librería de ejecución de Appimages:
  sudo apt install libfuse2

# Utilities de audio:
#  paplay - Playback a WAV file via a PulseAudio sink.
#  pacat - Cat raw audio data to a PulseAudio sink.
#  parec - Cat raw audio data from a PulseAudio source.
#  pacmd - Connect to PulseAudio's built-in command line control interface.
#  pactl - Send a control command to a PulseAudio server.
#  padsp - /dev/dsp wrapper to transparently support OSS applications.
#  pax11publish - Store/retrieve PulseAudio default server/sink/source settings in the X11 root window.
  sudo apt install pulseaudio-utils

# Utilidades de video:
  # https://manpages.debian.org/trixie/v4l-utils/v4l2-ctl.1.en.html
  # https://www.mankier.com/package/v4l-utils
  sudo apt install v4l-utils
  # Procesador de imagen, video y audio por línea de comando
  sudo apt install ffmpeg

# RIP (borrador de archivos con cementerio resucitable)
  wget https://github.com/MilesCranmer/rip2/releases/download/v0.9.6/rip-Linux-x86_64-musl.tar.gz
  tar -xf ./rip-Linux-x86_64-musl.tar.gz
  sudo mv ~/rip /usr/bin
  rm ./rip-Linux-x86_64-musl.tar.gz
  alias rm=rip

# fzf (FuZzy files, buscador de archivos)
  sudo apt install -y fzf

# dust (analizador de ocupación de disco, mas informativo que du)
  sudo apt install du-dust

# bat (cat, pero con resaltado de sintaxis) (despues hay que usar batcat, agregado a los aliases)
  sudo apt install bat

# tldr (resumidor de manuales de comandos)
  sudo apt install tldr-py

# FORMATEADORES DE INFORMACION DEL EQUIPO:
# Informacion general:
  sudo apt install fastfetch
# Información del CPU:
  sudo apt install cpufetch
# Rendimiento del CPU
  sudo apt install sysbench

# Utilities Android:
  sudo apt install android-sdk-platform-tools-common
  sudo apt install adb fastboot
  sudo cp /lib/udev/rules.d/51-android.rules /etc/udev/rules.d/
