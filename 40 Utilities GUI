# UTILITIES PARA LA INTERFAZ GRAFICA

# Catálogo de software:
  sudo apt install gnome-software
  sudo apt install flatpak
  sudo apt install gnome-software-plugin-flatpak    # para Gnome
  sudo apt install plasma-discover-backend-flatpak  # para KDE
  flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

# Acceso remoto (cliente)
  sudo apt install remmina

# VPN zerotier
  curl -s https://install.zerotier.com > zt.sh
  chmod +x zt.sh
  ./zt.sh
  
