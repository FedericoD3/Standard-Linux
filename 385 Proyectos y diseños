# PROGRAMAS QUE USO PARA PROYECTOS, DISEÑOS Y PROGRAMACION

# Como siempre, actualizar repositorios primero:
  sudo apt update
  sudo apt upgrade

# CudaText (sustituto de Notepad++ de Windows):
  wget https://sourceforge.net/projects/cudatext/files/release/1.234.3.1/cudatext_1.234.3.1-1_gtk3_amd64.deb/download -O cudatext.deb
  sudo apt install ./cudatext.deb
  rm ./cudatext.deb

# VSCodium (También via el catálogo "Discover" o "Software"):
  wget -qO - https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/raw/master/pub.gpg | gpg --dearmor | sudo dd of=/usr/share/keyrings/vscodium-archive-keyring.gpg
  echo 'deb [ signed-by=/usr/share/keyrings/vscodium-archive-keyring.gpg ] https://download.vscodium.com/debs vscodium main' | sudo tee /etc/apt/sources.list.d/vscodium.list
  sudo apt update && sudo apt upgrade
  sudo nala install codium

# KiCAD EDA más completo
  sudo apt install kicad

# Fritzing EDA mas básico, pictórico
  sudo apt install fritzing

# FreeCAD
# APPIMAGE:
#  descargar desde:
  https://github.com/FreeCAD/FreeCAD-Bundle/releases/tag/weekly-builds
#  al directorio ~/.local/share/applications/Freecad.appimage
# INSTALADO:
  sudo add-apt-repository ppa:freecad-maintainers/freecad-stable
  sudo nala update
  sudo nala install freecad

# ArduinoIDE:
  # Crear el acceso directo del menú a ~/Sync/PortApps/Linux/ArduinoIDE/arduino-ide.AppImage
  # y los enlaces a configuración y librerías:
    ln -s ~/Sync/PortApps/Linux/ArduinoIDE/≈∫.arduino15 ~/.arduino15
    ln -s ~/Sync/PortApps/Linux/ArduinoIDE/≈∫.arduinoIDE ~/.arduinoIDE
