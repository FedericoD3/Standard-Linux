# UTILITIES PARA LA INTERFAZ GRAFICA

# Remapeo de teclas:
  sudo nala install xte
  sudo nala install xautomation
  sudo nala install xbindkeys
  xbindkeys --defaults > .config/.xbindkeysrc

# Miscelaneos:
  sudo apt install gnome-disk-utility    # Para manejar los puntos de montaje de las particiones, segun preparados en "03 Estructura de directorios"
  sudo apt install gparted

# Administrador de credenciales locales
  sudo nala install seahorse

# Pipewire audio (mas moderno que Pulseaudio) https://linuxgenie.net/install-pipewire-debian-12/
  sudo apt install pipewire-audio wireplumber pipewire-pulse pipewire-alsa libspa-0.2-bluetooth
  pipewire --version
  systemctl --user --now enable wireplumber.service
  wireplumber --version
  sudo reboot

# Emulador de terminal menos basico que mlterm:
  sudo nala install xfce4-terminal

# Catálogo de software:
  sudo nala install gnome-software
  sudo nala install flatpak
    sudo nala install gnome-software-plugin-flatpak    # para Gnome
    sudo nala install plasma-discover-backend-flatpak  # para KDE
  flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

# Administrador de privilegios para programas instalados como Flatpack
  Flatseal (instalar por el catálogo "Discover")

# Acceso remoto (cliente)
  sudo nala install remmina

# Capturador de pantalla
  sudo nala install ksnip
  #  Menu > Preferences > Startup > agregar ksnip para que se active con PrtScn
  #    En Ksnip configurar 
  #  Menu > Preferences > Keyboard > System > Screenshots > 
  #   y borrar los hotkeys de captura de pantalla, para que los maneje ksnip

# Administrador de Zips:
  sudo nala install xarchiver

# Block de notas:
  sudo nala install gedit  # Disk space required  28.6 MB 
  sudo nala install kate   # Disk space required  180.9 MB (mas lento)
  sudo nala install geany  # Disk space required  13.4 MB

# Visores de imagenes:
  XnView MP  (instalar por el catálogo "Discover")
  # Y darle acceso con Flatseal a los directorios relevantes en el file system

# IPqalc   Calculadora de IPs
  sudo nala install ipqalc
