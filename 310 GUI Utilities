# UTILITIES PARA LA INTERFAZ GRAFICA

# Administrador de procesos:
  sudo apt install xfce4-taskmanager

# Editor de menu inicio:
  sudo apt install menulibre
# Editor de foto de usuario:
  sudo apt install mugshot

# Visor de imágenes multiplataforma (sirve en Windows)
  wget https://www.xnview.com/download.php?file=XnViewMP-linux-x64.deb -O XnViewMP-linux-x64.deb
  sudo apt install ./XnViewMP-linux-x64.deb
  rm XnViewMP-linux-x64.deb

# Block de notas similar a Notepad++ (mejor que Mousepad, similar a notepad)
  sudo apt install notepadqq 

# Remapeo de teclas:
  sudo apt install -y xte
  sudo apt install -y xautomation
  sudo apt install -y xbindkeys
  xbindkeys --defaults > .config/.xbindkeysrc
  sudo apt install -y xdotool

# Miscelaneos:
  sudo apt install -y gnome-disk-utility    # Para manejar los puntos de montaje de las particiones, segun preparados en "020 Estandarizacion de directorios"
  sudo apt install -y gparted               # Para crear, redimensionar, mover o eliminar particiones

# Administrador de credenciales locales
  sudo apt install -y seahorse

# Pipewire audio (mas moderno que Pulseaudio) https://linuxgenie.net/install-pipewire-debian-12/
  sudo apt install -y pipewire-audio wireplumber pipewire-pulse pipewire-alsa libspa-0.2-bluetooth
  pipewire --version
  systemctl --user --now enable wireplumber.service
  wireplumber --version
  sudo reboot

# Emulador de terminal menos basico que mlterm y uxterm:
  sudo apt install xfce4-terminal
# Emulador de terminal más completo que xfce4-terminal:
  sudo apt install tabby-terminal

# Catálogo de software:
  sudo apt install -y gnome-software
  sudo apt install -y flatpak
    sudo apt install -y gnome-software-plugin-flatpak    # para Gnome
    sudo apt install -y plasma-discover-backend-flatpak  # para KDE
  sudo flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

# Administrador de privilegios para programas instalados como Flatpack
  Flatseal (instalar por el catálogo "Discover")

# Acceso remoto (cliente)
  sudo apt install -y remmina

# Capturador de pantalla
  sudo apt install -y ksnip
  #  Menu > Options > Settings > Saver > Capture save location:
  #  ~/Pantallazos/$(date +%Y-%m)/$(date +%Y%m%d-%H%M%S).png
  #  para que las guarde en directorios por mes.
  # Si quieres que las capturas aparezcan en ~/Pantallazos pero que realmente estén en un disco para "bulto":
    ln -s /Discos/Resto/Sync/Pantallazos/$HOSTNAME ~/Pantallazos
  #  Menu > Options > Settings > Hotkeys
  #   Marcar "Enable global hotkeys"
  #   Rect Area:   Ctrl+Print
  #   Last Rect:   Alt+Shift+Print
  #   Full screen: Meta+Print (Todas las pantallas) (Meta es la tecla de Windows) 
  #   Curr screen: Print (solo la pantalla con foco)
  #   Active wind: Shift+Print

# Administrador de Zips:
  sudo apt install -y xarchiver

# Visores de imagenes:
  wget https://download.xnview.com/XnViewMP-linux-x64.deb
  sudo apt install XnViewMP-linux-x64.deb
  rm XnViewMP-linux-x64.deb
  XnView MP  (instalar por el catálogo "Discover")
  # Y darle acceso con Flatseal a los directorios relevantes en el file system

# Calculadora (también para Windows?!)
  sudo apt install -y qalculate-gtk

# IPqalc   Calculadora de IPs
  sudo apt install -y ipqalc

# Lupa:
  sudo apt install -y kmag

# Comparador de directorios
  sudo apt install -y meld

# Scanner de red
  wget https://github.com/angryip/ipscan/releases/download/3.9.1/ipscan_3.9.1_amd64.deb

# Compartidor de archivos por red:
  # Buscar la versión ma reciente en https://github.com/localsend/localsend/releases. A Septiembre 2025:
    wget https://github.com/localsend/localsend/releases/download/v1.17.0/LocalSend-1.17.0-linux-x86-64.deb -O Localsend.deb
    sudo apt install ./Localsend.deb
    rm Localsend.deb

# Examinadores de hardware:
  sudo apt install hardinfo hardinfo2 hwinfo
